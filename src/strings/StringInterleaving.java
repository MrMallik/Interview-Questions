/*
package strings;

public class StringInterleaving {

    public static boolean isInterleaving(String A, String B, String C){
        int aPtr = 0, bPtr = 0, cPtr = 0;

        if(C.length() != A.length() + B.length())
            return false;

        while(cPtr < C.length()){

            while(A.charAt(aPtr) == C.charAt(cPtr)){
                aPtr++;
                cPtr++;
            }

            while(B.charAt(aPtr))

        }r
    }

}
*/
